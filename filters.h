

//const float cutoff_freq   = 1.0;      //Cutoff frequency in Hz
//const float sampling_time = 0.005;    //Sampling time in seconds.
//IIR::ORDER  order  = IIR::ORDER::OD3; // Order (OD1 to OD4)
//
//LP_filter_cap0 = Filter(cutoff_freq, sampling_time, order);
//LP_filter_cap1 = Filter(cutoff_freq, sampling_time, order);
//LP_filfter_EMG = Filter(cutoff_freq, sampling_time, IIR::ORDER::OD4);
