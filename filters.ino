//#include "Filters.h"

//float LP_filter(Filter *LPfilter, int raw_val){
//  // Applies the filter given as input to the raw data input and returns the filtered value.
//  float filtered_val = LPfilter.filterIn(raw_val);
//  return filtered_val;
//}
